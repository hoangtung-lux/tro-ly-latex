{
  "name": "tro-ly-latex",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp index.html .vercel/output/static/index.html && echo 'Build complete'"
  }
}
