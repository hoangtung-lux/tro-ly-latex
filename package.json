{
  "name": "tro-ly-latex",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r styles dist/ && cp -r scripts dist/ && cp -r data dist/ && echo 'Build complete'"
  }
}
