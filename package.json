{
  "name": "tro-ly-latex",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "dev": "npx -y serve .",
    "build": "npx shx rm -rf dist && npx shx mkdir -p dist && npx shx cp index.html dist/index.html && npx shx cp -r styles dist/ && npx shx cp -r scripts dist/ && npx shx cp -r data dist/ && echo Build complete"
  }
}
